# 5c91 Spec production caller ledger

Scope: the production entries named by board 5c91 at `10540ac38`, after aa84, 9219, e363, and b645. A row marked `unchanged` was checked against its actual call path; it is not an import-count assertion. The authoritative transition is the saved Step Result and Definition Settlement, with the Store applying their exact receipt. `SpecStep`, `SpecReviewStep`, `SpecTriageStep`, `SpecRepairStep`, and `SpecGateStep` own the respective semantic choices; `settleSpecStepResult` owns their Settlement and connector choices.

Categories: **E** executes a selected Action and validates an external boundary; **S** obtains canonical input and coordinates I/O through a Service; **P** projects or applies a saved Result/Settlement; **A** admits an exact current action; **N** retains the established resolver for a non-Spec phase. Rows with two categories have both actual responsibilities.

| Production caller | Category and production route | Spec authority / non-Spec owner | Change | Covering test |
| --- | --- | --- | --- | --- |
| `run-gate` | E,A; direct Gate evaluation and dispatcher result | `SpecGateStep` / `settleSpecStepResult`; non-Spec `resolveGateTransition` | unchanged | `spec-gate-command-boundary.test.js` (direct/dispatch, stale payload, tooling output) |
| `run-settle-gate-transition` | N,A; explicit exhausted integration/Task Gate action | non-Spec `resolveGateTransition` | removed stale Spec branch | `canonical-flow-manager-runtime.test.js` (integration defer and repeat refusal) |
| `run-repair-plan-gate` | N,A; explicit Task repair action | non-Spec `resolveGateTransition`; Spec repair stays in `SpecGateStep` and Store | removed unreachable Spec resolver arm after the existing StepResult-only refusal | `canonical-flow-manager-runtime.test.js` (repair admission), `spec-gate-result-settlement.test.js` (Spec repair) |
| `run-claim-next-action` | A,N; claim the projected ordinary or non-Spec Gate retry | saved Spec Result/Settlement; non-Spec `resolveGateTransition` | removed stale Spec retry branch | `get-next-action.test.js` (claim), `spec-gate-command-boundary.test.js` (atomic Spec retry projection) |
| `gate-transition-application` | P,N; project/apply a sealed non-Spec Gate plan | non-Spec `resolveGateTransition` | unchanged | `gate-transition-boundary.test.js` (plan projection and application) |
| `gate-transition-facts` | S,A; current catalog, Attempt and prospective evaluator facts | `SpecGateStep` uses prospective facts; non-Spec Definition consumes persisted facts | unchanged | `spec-gate-result-settlement.test.js` (binding/refusal), `gate-transition-boundary.test.js` (facts) |
| `canonical-gate-artifacts` | E,S; typed evaluator promotion and catalog binding | `SpecGateStep` / `SpecGateService` | unchanged | `spec-gate-command-boundary.test.js`, `spec-gate-result-settlement.test.js` |
| `canonical-gate-observation-cycle` | S; canonical recurrence input for Gate repair | `SpecGateStep` / `SpecGateService`; Store records selected outcome | unchanged | `canonical-gate-observation-cycle.test.js`, `spec-gate-result-settlement.test.js` |
| `gate-observation-convergence` | S; typed observation comparison supplied to Gate Service | `SpecGateStep` decides Result; Definition decides route | unchanged | `gate-observation-convergence.test.js` |
| `get-next-action` | P,N; read saved Spec settlement and project directive | saved `SpecGateStep` Result/Settlement; non-Spec Gate resolver | unchanged | `spec-gate-command-boundary.test.js` (retry without old claim/reconcile), `get-next-action.test.js` |
| registry Gate `post` | E,A,P; dispatch attached result to Spec Step before generic Gate publication | `SpecGateStep` / `SpecGateService` / Store | rejects mismatched attachment or forged non-Spec phase against the active Spec Gate before generic publication; preserves admission-refusal classification through dispatcher fallback | `spec-gate-command-boundary.test.js` (direct/dispatch, rollback, both mismatch cases) |
| registry Gate `onError` | E,N; preserve non-Spec failure lifecycle | Spec uses Step failure and bypasses generic hook | unchanged | `spec-gate-command-boundary.test.js` (tooling error), `gate-transition-boundary.test.js` |
| `nonblocking` | P,A; saved Spec Result eligibility and exact decision admission | `SpecGateStep` Result; Definition-selected advisory effect | unchanged | `spec-gate-result-settlement.test.js` (strict/advisory), `nonblocking.test.js` (identity/replay) |
| `nonblocking-route` | P; static acceptance-backed destination projection | saved Spec Result and Definition route; other phases retain their routes | unchanged | `nonblocking.test.js` (Spec and Task routes) |
| `run-review` | E,S; bound Spec Review provider, publication and recovery | `SpecReviewStep` / `SpecReviewService` / `settleSpecStepResult` | unchanged | `canonical-flow-manager-runtime.test.js` (normal, restart, overlap), `run-review-execution-admission.test.js` |
| `review-transition-persistence` | N,P; non-Spec Review fact and selected deferral application | Spec Review explicitly uses `SpecReviewService` | unchanged | `canonical-flow-manager-runtime.test.js` (Spec bypass, impl deferral) |
| `run-settle-review-transition` | N,A; explicit non-Spec Review deferral | non-Spec `resolveReviewTransition`; Spec uses saved Result | unchanged | `canonical-flow-manager-runtime.test.js` (impl Review deferral and restart) |
| `worker-artifact-handoff` | E,S,A; sealed Spec candidate, exact Attempt and cleanup recovery | `SpecStep`/`SpecTriageStep`/`SpecRepairStep` via their Services and `settleSpecStepResult` | unchanged | `worker-artifact-handoff.test.js` (adopted candidate, replay, cleanup recovery, finding input) |
| `flow-findings` | S,A; one Store for publication and four-field source resolution | Store identity `sourceArtifact + sourceStep + sourceFindingId + fingerprint` | unchanged | `flow-findings.test.js`, `worker-artifact-handoff.test.js` |
| `canonical-acceptance-artifacts` | S; deferred source readback for Acceptance evidence | `CanonicalFlowFindingsStore.resolveFinding` | strengthened two-entry test | `run-acceptance-review-source.test.js` (same ID, distinct fingerprints and missing-source refusal) |
| `artifact-view-reader` | P; read-only cataloged deferred source projection | `CanonicalFlowFindingSourceArtifact.resolveFinding` | added four-entry reader test | `artifact-view.test.js` (DF-1–DF-4, unchanged catalog/source bytes) |
| dispatcher (`run-dispatch`) | E,A; execute the Definition-selected command and verify its boundary result | selected Spec Step Service/Store; no independent semantic completion | unchanged | `spec-gate-command-boundary.test.js` (direct/dispatch parity), `dispatcher-command-owner.test.js` |
| `canonical-flow-manager-store` Gate API | P,A; atomic Spec Result, publication, receipt, route and replay | `SpecGateStep` / `settleSpecStepResult`; non-Spec Gate plan | rejects migrated Spec in generic Gate decision API | `spec-gate-result-settlement.test.js` (stale Result refusal, retry, defer, replay), `canonical-flow-manager-runtime.test.js` (non-Spec Gate) |
| `canonical-flow-manager-store` Review API | P,A; exact Spec Review execution/publication settlement | `SpecReviewStep` / `settleSpecStepResult`; non-Spec Review plan | unchanged | `canonical-flow-manager-runtime.test.js` (restart/replay/conflict/Error Result) |
| `canonical-flow-manager-store` no-progress API | P,A; persist selected Spec plan Gate repair no-progress outcome | `SpecStep` / `settleSpecStepResult` and exact repair binding | unchanged | `worker-artifact-handoff.test.js` (Spec no-progress Result and receipt), `canonical-flow-manager-runtime.test.js` (shared Task no-progress rejection) |

## Completion and identity checks

| Outcome | Production path and authority | Evidence |
| --- | --- | --- |
| Direct Spec Gate / dispatch | Both enter registry `post`, call `SpecGateStep`, and settle through `SpecGateService` and Store; public result is matched to the saved Result/receipt. | `spec-gate-command-boundary.test.js` direct/dispatch parity and fault rollback |
| Spec Review direct / publication recovery | Both use `SpecReviewService.completePublication`; terminal replay checks the exact receipt and publication after manager restart. | `canonical-flow-manager-runtime.test.js` normal, restart, overlap and conflicting terminal Result |
| Spec worker cleanup recovery | The sealed handoff readback and its saved settlement authorize cleanup; no Step is reopened to infer completion. | `worker-artifact-handoff.test.js` completed namespace and interruption recovery |
| Spec Gate publication-only recovery | `resolveGatePublicationRecovery` returns `null` for `spec` and `task-spec`; non-Spec phases retain publication reconciliation. | `gate-transition-boundary.test.js` publication recovery, `spec-gate-command-boundary.test.js` atomic retry |
| Same-ID deferred findings | `flow.findings` preserves distinct four-field identities. Spec worker input, Acceptance evidence, and Artifact View resolve through the common typed source reader. DF-1–DF-4 in the Artifact View fixture share a source ID but have four distinct fingerprints and four distinct original observations. | `flow-findings.test.js`, `worker-artifact-handoff.test.js`, `run-acceptance-review-source.test.js`, `artifact-view.test.js` |

The board's “Issue #521 migration-parity” reference does not match the current [GitHub issue #521](https://github.com/SpreadWorks/sennel/issues/521) (Flow help). The four-finding check above uses the requested DF-1–DF-4 observation format without treating an unrelated issue as authority. The old `specs/311-final-regression-proceed/001/steps/flow-findings.json` is version 1 and is not used as a current canonical fixture.

The responsibility and state tables in `src/flow/AGENTS.md` remain aligned: Review keeps checkpoint → claim → publication → terminal order, Spec Gate has no publication-only reconcile, and Task 4's nonblocking owner remains unchanged. The audit found no retired schema or fixture needed by a production Spec path. The live stale generic Spec Gate claim/settle arms and an unreachable repair resolver arm were removed above.

The adjacent `set-retry.test.js` suite has three pre-existing failures (`AGENT_AUTHENTICATION_FAILED`, `AGENT_USAGE_LIMIT_REACHED`, `SUBPROCESS_FAILURE`: expected `INVALID_RECOVERY_INPUT`, received `RETRY_NOT_AVAILABLE`). The same 53-pass/3-fail result was reproduced at the untouched `10540ac38` HEAD in a temporary detached worktree; no retry production or test code was changed for this board.

Final scoped runs: Gate/Store/nonblocking 201/201; Review/worker 178/178; lifecycle/dispatcher 104/104; identity readback 21/21; Gate/Step/identity units 34/34; Task repair 15/15. The admission regression first detected a canonical-state mutation before the product fix, then passed with both wrong-key and forged-phase inputs after the fix.

`SpecStep` chooses and adopts the prepared worker candidate; `SpecTriageStep` and `SpecRepairStep` choose their permitted deltas; `SpecGateStep` selects its result from prospective facts. Their Services verify those exact selections, obtain catalog inputs and call `settleSpecStepResult` once. The Definition owns target and connector selection. Store replay reads the existing receipt and rejects changed binding, Result or publication; admission and persistence exceptions are not turned into semantic Error Results.
