{
  "goal": "Workspace / Connector が内部 filesystem layout に依存せず、選択した canonical Flow Version の metadata または confirmed Activities を個別に取得できる read-only な `sennel flow query` 公開 API を提供する。",
  "background": "Issue #517 は、既存の canonical runtime が保持する Flow Version identity、検証済み current state、confirmed Activity history、Artifact catalog の authority を、Workspace / Connector 向けの安定した query 境界として公開することを求めている。単一 snapshot の再構成ではなく、`metadata` と `activities` を別 resource として同じ Spec / Flow Version に対して取得する。Fresh Flow と migrated Flow は同じ validated reader を通し、証明できない creation timestamp は推測せず unavailable とする。",
  "scope": {
    "in": [
      "`sennel flow query` の command routing、stdin / `--request-file` 入力、単一 JSON response の出力",
      "`metadata` と `activities` の request validation、Spec / Flow Version の discovery と選択",
      "canonical state、Spec record、Activity ledger、Artifact catalog からの public projection",
      "Schema Revision 1 の response / error contract、stable error code、CLI exit status",
      "`confirmationOrder` 昇順の Activity filter / cursor pagination、append stability",
      "fresh / migrated、active / parked / derived blocked / finalized、Git あり / なしの contract fixture と consumer compatibility 検証"
    ],
    "out": [
      "Workspace 固有 view model や metadata / activities の再合成",
      "Artifact bodies、raw logs、prompt bodies、internal paths、internal file names、raw catalog、checkpoints、transactions、locks、temporary data の公開",
      "cross-Spec search、full-text search、arbitrary sort、offset pagination、total count、Activity aggregate query",
      "Flow Version の作成・increment、Version 2 以降の作成",
      "old layout parser、migration classifier / report 依存、backward-compatible fallback",
      "query による Flow state、Activity ledger、Artifact storage、cache、Git working tree の変更"
    ]
  },
  "constraints": [
    "Production input は canonical Version Store、`flow.json`、`spec.json`、Activity ledger、Artifact catalog に限定し、canonical authority を唯一の状態 source とする。",
    "Fresh Flow と migrated Flow は同じ validated reader / validator を通し、旧 layout や migration report を query 側で直接解析しない。",
    "Lifecycle の公開値は `active`、`parked`、`finalized` に限定し、`blocked` は current Attempt failure と `nextAction().operation === \"blocked\"` から導出する。",
    "意味のある request、selector、datetime range、cursor、response、error、timestamp availability、Artifact descriptor、metrics は専用 class とし、constructor で invariant を検証する。",
    "Node.js 組み込みモジュールのみを使用し、query 実行時に Codex、Claude Code、IDE、agent-host lifecycle 機能へ依存しない。",
    "公開 response は query 専用 top-level schema とし、既存の generic Flow Envelope を流用しない。",
    "request は `resource` と `condition` を必須とし、request 自体に schema revision、`cardinality`、`order`、as-of 指定を持たせない。",
    "Version 選択は一つの non-empty `condition.specId` と positive safe integer の `condition.flowVersion` で行い、省略時は `1` を選ぶ。存在しない明示 Version へ別 Version を fallback しない。",
    "Activity の公開時刻は authority-backed `timing.finishedAt` とし、filesystem timestamp を利用しない。",
    "query は successful / failed のいずれでも canonical files と Git working tree を変更しない。"
  ],
  "design_principles": [
    "Canonical authority を直接読む深い query module に projection、validation、read-only 境界を集約する。",
    "Public projection は必要な field group だけを明示し、internal storage model の漏出を型と schema の両方で防ぐ。",
    "Activity ledger の confirmed prefix と contiguous `confirmationOrder` を pagination の位置 authority として扱う。",
    "選択、順序、timestamp、error、cursor binding を centrally managed contract から一貫して適用する。",
    "Reader と projector を fresh / migrated の双方で共有し、source layout に依存する分岐を増やさない。",
    "Version identity と location は専用 value object で扱い、Version 1 の暗黙 hardcode を query の Version discovery に持ち込まない。"
  ],
  "overview": {
    "modules": [
      {
        "text": "Query command adapter: `sennel flow query` の stdin / `--request-file` 境界、JSON-only stdout、exit status を担当する。"
      },
      {
        "text": "Query request value objects: resource、Spec / Flow Version selector、datetime range、page、opaque cursor を検証する。"
      },
      {
        "text": "Canonical Version discovery / reader: Spec ごとの available Version を発見し、選択 Version の state、Spec、Activity、Artifact authorities を読み取る。"
      },
      {
        "text": "Metadata projector: identity、lifecycle、location、structure、relationships、timestamps、capabilities、artifacts、metrics を public schema に写像する。"
      },
      {
        "text": "Activity projector: confirmed Activity prefix の identity、transition、timing、usage、outcome、failure、reference を public schema に写像する。"
      },
      {
        "text": "Artifact descriptor projector: catalog の internal path / body を隠し、deterministic `artifactId` と schema-defined metadata / relations だけを返す。"
      },
      {
        "text": "Response / error contract: Schema Revision 1 の resource-specific success shape、error shape、centrally managed error code を所有する。"
      },
      {
        "text": "Cursor codec / validator: resource、Spec、選択 Version、datetime bounds、最終 `confirmationOrder` に binding した opaque cursor を encode / validate する。"
      },
      {
        "text": "Contract fixture / consumer compatibility layer: lifecycle、migration、Git、Version discovery、filter、pagination、error、read-only を behavior-level に検証する。"
      }
    ],
    "data_flow": [
      {
        "text": "stdin または `--request-file` → 単一 JSON object parser → resource-specific request class。"
      },
      {
        "text": "request class → canonical Version discovery → `specId` の available Version 昇順列と選択 Version を確定する。"
      },
      {
        "text": "選択 Version → 同一の validated reader → canonical state、Spec record、confirmed Activity prefix、Artifact catalog を読み込む。"
      },
      {
        "text": "`metadata` → Metadata projector → `item` を一つ持つ Schema Revision 1 success response。"
      },
      {
        "text": "`activities` → timing filter → cursor position filter → `confirmationOrder` 昇順 Activity projector → `items` / `pageInfo` response。"
      },
      {
        "text": "reader / validation error → resource-specific error projector → `ok: false` response と non-zero exit。"
      },
      {
        "text": "response schema → consumer compatibility fixture が `schemaRevision`、resource cardinality、Version list、公開 field 境界を検証する。"
      },
      {
        "text": "全 query path → read-only guard → canonical digest と Git status の before / after 比較で副作用なしを確認する。"
      }
    ],
    "decisions": [
      {
        "text": "[VERIFY] Draft の typed Flow identity / Version location authority は既存の専用 value object と一致する。",
        "evidence": "`src/lib/flow-version.js` の `FlowVersion`、`FlowVersionId`、`FlowSpecIdentity`、`FlowVersionRelativeLocation`、`FlowVersionLocation` が positive Version と canonical repository-relative location を constructor で検証する。",
        "consideredAlternatives": "文字列 path や数値の直接結合は既存の authority boundary と invariant を迂回するため採用しない。"
      },
      {
        "text": "[VERIFY] Draft の lifecycle 制約と derived blocked 表現は現行 state model と一致する。",
        "evidence": "`src/flow/lib/current-flow-state.js` の `CurrentFlowLifecycle` は `active` / `parked` / `finalized` のみを受理し、`CurrentFlowState.nextAction()` は failure disposition の `operation` を返し、blocked disposition を定義している。",
        "consideredAlternatives": "`blocked` を lifecycle enum に追加する案は current Attempt failure と definition-owned next action の authority を二重化するため採用しない。"
      },
      {
        "text": "[VERIFY] Draft の confirmed Activity prefix、contiguous order、finished timestamp authority は現行 ledger model と一致する。",
        "evidence": "`src/flow/lib/current-flow-state.js` の `CurrentFlowStateSnapshot` は `state.confirmationOrder` までの Activity だけを保持し、`FlowActivity` / `ActivityTiming` は `confirmationOrder` と timezone-bearing `finishedAt` を検証する。",
        "consideredAlternatives": "filesystem mtime や未確認 journal suffix を公開時刻・Activity として使う案は state authority と一致しないため採用しない。"
      },
      {
        "text": "[VERIFY] Draft の creation evidence policy と `flow_created` authority は現行 model と一致する。",
        "evidence": "`src/flow/lib/current-flow-state.js` の `CurrentFlowCreationAuthority` は available source または reason 付き unavailable を表現し、`FlowActivity.flowCreated()` は最初の creation timing と deterministic Activity identity を生成する。",
        "consideredAlternatives": "migrated Flow の creation time を file timestamp や query 実行時刻から推測する案は採用しない。"
      },
      {
        "text": "[VERIFY] Draft の Artifact catalog authority と Activity association policy は現行 model と一致する。",
        "evidence": "`src/lib/flow-version.js` の `FlowArtifactDescriptor`、`FlowArtifactCatalog`、`FlowArtifactActivityAssociation`、`FlowArtifactCatalogStore` は descriptor、hash、logical contract、Activity association、canonical file verification を検証する。",
        "consideredAlternatives": "catalog の raw descriptor や Artifact body をそのまま公開する案は internal storage boundary を越えるため採用しない。"
      },
      {
        "text": "[CORRECTION] 現行 `CanonicalFlowRuntime` / `CurrentFlowVersionStore` は Version 1 専用なので、query の Version discovery は既存 Version 1 facade の caller option 追加だけで実装しない。",
        "evidence": "`src/flow/lib/canonical-flow-runtime.js` は `<specRoot>/<specId>/001` のみを解決し、`src/flow/lib/current-flow-state.js` の `CurrentFlowVersionStore` は Version 1 以外を拒否する。一方、`src/lib/flow-version.js` の `FlowVersion` / `FlowVersionLocation` は positive Version を表現できる。",
        "consideredAlternatives": "Version 1 固定のまま `availableFlowVersions` を返す案は Issue #517 の Version 2 以降 fixture 要件と矛盾するため採用しない。"
      },
      {
        "text": "Schema Revision 1 は resource ごとの cardinality と固定順序を top-level contract として持つ。",
        "evidence": "Issue #517 と draft は metadata の `item`、activities の `items` / `pageInfo`、`selectedFlowVersion`、ascending `availableFlowVersions` を必須とする。",
        "consideredAlternatives": "既存 generic Flow Envelope、request-side `cardinality`、request-side `order` は consumer contract を曖昧にするため採用しない。"
      },
      {
        "text": "Cursor は versioned canonical JSON payload を deterministic に base64url serialize し、resource、selector、bounds、last order を保持する。",
        "evidence": "Issue #517 は opaque cursor と query binding を要求し、内部表現は指定していない。Node.js built-in の JSON / base64url / SHA-256 で追加 dependency なしに実装できる。",
        "consideredAlternatives": "offset や filesystem position は append-only ledger の confirmed `confirmationOrder` と query binding を表現できないため採用しない。"
      },
      {
        "text": "Cost は provider が実測した non-negative amount と currency を持つ場合だけ公開し、token count から推計しない。",
        "evidence": "Issue #517 と draft は measured-only cost を明示し、現行 `ActivityUsage` は nullable `cost` を受理する。",
        "consideredAlternatives": "token 数、model list price、経過時間からの推計値は authority-backed measurement ではないため採用しない。"
      },
      {
        "text": "Resource が確定できない validation error は resource-specific payload を省略し、known resource error は安定した空 payload を返す。",
        "evidence": "Issue #517 は metadata error の `item: null`、activities error の空 `items` / `pageInfo`、未知 resource の resource-specific payload omission を要求する。",
        "consideredAlternatives": "partial metadata / partial Activity を成功 response に混在させる案は不整合な consumer state を作るため採用しない。"
      }
    ]
  },
  "requirements": [
    {
      "id": "R1",
      "desc": "`sennel flow query` は top-level command として専用 query dispatch path に入り、stdin または `--request-file` のどちらか一方から exactly one JSON object を受ける。同時指定、入力なし、trailing value、multiple value、unknown field、resource 不適合 field を拒否し、query-specific Schema Revision 1 response の JSON だけを stdout に出す。汎用 Flow Envelope、ambient Flow lifecycle、runtime log、post-hook はこの dispatch path では使用しない。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R2",
      "desc": "request は `metadata` / `activities` の resource と non-empty `condition.specId` を必須とし、`condition.flowVersion` の省略時は `1`、明示時はその positive safe integer だけを選択する。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R3",
      "desc": "canonical Store から対象 Spec に属する canonical directory/version identity (`FlowVersionLocation`) を列挙する non-migration、read-only `CanonicalFlowVersionReader.listAvailableVersions(specId)` を用い、response と Version-not-found error に unique ascending `availableFlowVersions` を含める。`condition.flowVersion` は directory identity を選び、指定がなければ 1、明示指定が存在しなければ fallback なしで `FLOW_VERSION_NOT_FOUND` とする。state の `version` は別の format revision であり、選択 Version ではない。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R4",
      "desc": "Schema Revision 1 の metadata success は exact top-level keys `{ schemaRevision: 1, ok: true, resource: \"metadata\", selectedFlowVersion: { specId: string, flowVersion: positive safe integer }, availableFlowVersions: unique ascending positive safe integer[], item: MetadataItem }` を返す。`item` は必ず一つで、`items`、request-side `cardinality`、request-side `order`、state format revision を返さない。`flowVersion` と `availableFlowVersions` は canonical directory/version identity を示す。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R5",
      "desc": "metadata の `item` は exact public whitelist `{ identity, lifecycle, location, structure, relationships, timestamps, capabilities, artifacts, metrics }` を持つ。`identity` は `{ specId: string, flowVersion: positive safe integer }`、`lifecycle` は `{ status: \"active\"|\"parked\"|\"finalized\", blocked: boolean }`、`location` は public Git availability/commit metadata のみ、`structure`/`relationships` は public node/task IDs のみ、`timestamps` は authority-backed Timestamp values、`artifacts` は ArtifactDescriptor[]、`metrics` は AggregateMetrics とする。internal path、catalog、body、raw state は field whitelist の外に置く。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R6",
      "desc": "`timestamps.created` は confirmed prefix の最初の `transition.operation === \"flow_created\"` Activity の `timing.finishedAt`、`timestamps.updated` は state を変更した confirmed Activity のうち最新の `timing.finishedAt`、`timestamps.finalized` は最新の `transition.operation === \"finalize_flow\"` Activity の `timing.finishedAt` から作る。各値は `{ value: ISO8601|string|null, availability: \"available\"|\"unavailable\", reason: string|null, provenance: string|null }` とし、証拠がなければ filesystem timestamp/query time を使わず non-empty reason/provenance 付き unavailable にする。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R7",
      "desc": "public `ArtifactDescriptor` は exact keys `{ artifactId: string, metadata: { logicalKey: string|null, schemaRevision: positive integer|null, mediaType: string|null }, activityIds: string[], nodeIds: string[], taskIds: string[] }` を持つ。`artifactId` は canonical descriptor identity から deterministic に生成し、relation arrays は ID のみを返す。`relativePath`、internal filename、raw catalog、Artifact body、未検証 descriptor field は公開しない。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R8",
      "desc": "Schema Revision 1 の activities success は exact top-level keys `{ schemaRevision: 1, ok: true, resource: \"activities\", selectedFlowVersion: SelectedFlowVersion, availableFlowVersions: positive safe integer[], items: ActivityItem[], pageInfo: { limit: integer, endCursor: string|null, hasNext: boolean } }` を返し、`item` を返さない。`items` は validated reader が保持する confirmed prefix のみを `confirmationOrder` 昇順で投影し、request の bounds/filter/cursor はその列を同順で絞る。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R9",
      "desc": "activities の `page.limit` は 1..100 の integer、初回 `page.after` は null、`recordedAt.gte` / `lt` は timezone-bearing ISO 8601 とし、`timing.finishedAt` に対して比較する。0 件は success の空配列とする。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R10",
      "desc": "cursor は resource、`specId`、selected Version、両 datetime bound、最後の `confirmationOrder` に binding し、malformed cursor と別 query への再利用をそれぞれ `INVALID_CURSOR` / `CURSOR_QUERY_MISMATCH` として拒否する。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R11",
      "desc": "各 `ActivityItem` は exact public keys `{ activity: { id: string, type: string }, node: { id: string, key: string }, task: { id: string, key: string }|null, attempt: { id: string, sequence: positive integer }|null, sequence: positive integer|null, confirmationOrder: positive integer, transition: { operation: string, status: string|null }, timing: Timing|null, usage: Usage|null, outcome: Outcome|null, failure: Failure|null, blocker: Blocker|null, incomplete: Incomplete|null, metric: Metric|null, note: string|null, evaluationIds: string[], findingIds: string[], repairIds: string[], artifactIds: string[] }` を持つ。Timing/Usage/Outcome/Failure/Blocker/Incomplete/Metric は canonical typed values の whitelist projection とし、raw logs、prompt、checkpoint、transaction、lock、temporary data、Artifact body、その他の transition payload を返さない。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R12",
      "desc": "known-resource error は Schema Revision 1、resource、selected Version、unique ascending available Version list、`{ code, path, message }` を持ち、metadata では `item: null`、activities では空 `items` と `{ limit, endCursor, hasNext }` の `pageInfo` を返す。`path` は内部 storage path ではなく次の public request domain のいずれか (`/request`, `/resource`, `/condition/specId`, `/condition/flowVersion`, `/page/limit`, `/page/after`, `/recordedAt/gte`, `/recordedAt/lt`, `/canonical`) とし、message/path に filesystem path、file name、catalog key、lock name を含めない。invalid JSON など resource 不明 error は resource-specific payload を省略する。Canonical reader の parse、permission、missing required record は `CANONICAL_RECORD_UNREADABLE`、identity、digest、schema、contiguous ledger、catalog relation の不一致は `CANONICAL_RECORD_INCONSISTENT` とし、その他の request/spec/version/cursor failures は R13 の対応 code と public path を使う。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R13",
      "desc": "error code は centrally managed enum とし、`INVALID_JSON`→`/request`、`INVALID_REQUEST`→request field、`SPEC_NOT_FOUND`→`/condition/specId`、`FLOW_VERSION_NOT_FOUND`→`/condition/flowVersion`、`CANONICAL_RECORD_UNREADABLE`→`/canonical`、`CANONICAL_RECORD_INCONSISTENT`→`/canonical`、`INVALID_CURSOR`→`/page/after`、`CURSOR_QUERY_MISMATCH`→`/page/after` に固定する。parse/read/permission failures は UNREADABLE、identity/digest/schema/ledger/catalog integrity failures は INCONSISTENT に分類し、message/path は内部 storage path/name を redaction する。`ok: false` では CLI を non-zero で終了する。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R14",
      "desc": "active、parked、derived blocked、finalized の Version を success 対象とし、blocked は non-success lifecycle ではなく public derived data として返す。Finalized の同条件 query は repeated read で同一結果を返す。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R15",
      "desc": "fresh / migrated Flow を同じ non-migration `CanonicalFlowVersionReader` に渡し、Version 1 専用 facade を経由せず、Version 2 以降を含む `FlowVersionLocation` の discovery と selected Version の open/validation を行う。state、Spec record、confirmed Activity prefix、Artifact catalog は全 Version で同一の validated reader boundary を通し、query code / schema に Version 1 hardcode、old layout parser、migration report 依存を持たせない。",
      "priority": "must",
      "testable": true
    },
    {
      "id": "R16",
      "desc": "`flow query` は successful/failed の全経路で side-effect-free とする。専用 reader は canonical bytes を read-only open し、lock を取得・materialize せず、canonical file、Version-local `.runtime/locks` / owner file、cache、temporary data、Activity ledger、runtime log、Git working tree を作成・更新・削除しない。acceptance test はこれらの digest、存在、内容、Git status を前後比較し、generic Flow lifecycle と post-hook が呼ばれないことも確認する。",
      "priority": "must",
      "testable": true
    }
  ],
  "acceptance_criteria": [
    "`sennel flow query` は stdin / `--request-file` の両入力境界を守り、stdout に query-specific Schema Revision 1 response だけを返す。",
    "`metadata` は exactly one `item`、`activities` は `items` / `pageInfo` を返し、request は `cardinality` / `order` を要求しない。",
    "単一 `specId`、Version 省略時の Version 1、明示 Version の no-fallback、selected Version、ascending available Version list が全 resource と error shape で一貫する。",
    "active / parked / derived blocked / finalized、fresh / migrated、Git あり / なしの fixture が同じ canonical reader で検証される。",
    "metadata は current state、Spec、public Artifact relations、capabilities、aggregate metrics、creation / update / finalize timestamp availability を公開する。",
    "activities は confirmed Activity prefix を `timing.finishedAt` filter と `confirmationOrder` 昇順で公開し、0 件を success とする。",
    "cursor pagination は欠落・重複なしで取得でき、query 外の再利用・改変を機械可読 error とする。",
    "finalized の repeated read は stable で、active ledger append 後も既読 Activity の order と cursor position は変わらない。",
    "public response に `relativePath`、internal filename、raw catalog、raw log、prompt、checkpoint、transaction、lock、temporary data、Artifact body が現れない。",
    "invalid request / JSON / cursor、存在しない Spec / Version、unreadable / inconsistent canonical record は partial success なしの安定した error shape と non-zero exit になる。",
    "Version 2 以降の canonical fixture を追加しても query の Version 1 hardcode を変更せず取得できる。",
    "成功・失敗 query の前後で canonical file digest と Git status が不変である。"
  ],
  "clarifications": [
    {
      "q": "同じ Flow Version の `metadata` と `activities` を一回の response にまとめるか。",
      "a": "まとめず、用途の異なる `metadata` と `activities` を独立 resource として取得する。request に resource を一つだけ持たせ、resource-specific cardinality を固定する。"
    },
    {
      "q": "Version 1 以外を指定したときに存在する別 Version を返すか。",
      "a": "返さない。指定 Version が存在しなければ `FLOW_VERSION_NOT_FOUND` とし、発見済みの `availableFlowVersions` だけを案内する。"
    },
    {
      "q": "migrated Flow に creation evidence がない場合の timestamp をどう扱うか。",
      "a": "filesystem timestamp や query 時刻を推測に使わず、`unavailable` と non-empty reason / provenance を返す。"
    },
    {
      "q": "Activity の cursor はどの位置と query 条件に結び付けるか。",
      "a": "resource、`specId`、selected Version、`recordedAt.gte` / `lt`、最後の `confirmationOrder` に結び付け、malformed と binding mismatch を別 error code にする。"
    }
  ],
  "alternatives_considered": [
    {
      "option": "単一 snapshot resource を返す。",
      "reason": "Workspace / Connector が不要な resource まで取得し、内部 layout または再構成 view model に依存するため、`metadata` と `activities` の独立 resource を採用する。"
    },
    {
      "option": "既存の generic Flow Envelope を response に流用する。",
      "reason": "query-specific schema の cardinality、selected Version、available Version、resource-specific error shape を明確に保証できないため採用しない。"
    },
    {
      "option": "Version 1 に fallback するか、利用可能な最初の Version を返す。",
      "reason": "明示選択の意味を壊し、consumer が要求していない Version を読むため採用しない。"
    },
    {
      "option": "offset pagination または request-side sort を提供する。",
      "reason": "confirmed contiguous `confirmationOrder` と append stability を利用できず、cursor query binding の contract を弱めるため採用しない。"
    },
    {
      "option": "token 数や model 情報から cost を推計する。",
      "reason": "provider-measured amount / currency という authority がなく、metrics を不正確にするため採用しない。"
    },
    {
      "option": "old layout、migration report、filesystem timestamp を query source に含める。",
      "reason": "canonical Store を唯一の authority とする境界を破り、fresh / migrated の reader を分岐させるため採用しない。"
    }
  ],
  "open_questions": [],
  "keywords": [
    "flow-query",
    "metadata",
    "activities",
    "FlowVersion",
    "CurrentFlowStateSnapshot",
    "confirmationOrder",
    "FlowActivity",
    "artifact-catalog",
    "cursor-pagination",
    "finishedAt",
    "flow_created",
    "canonical-store",
    "read-only",
    "Schema-Revision-1",
    "Workspace"
  ],
  "implementationTargets": [
    "src/flow.js",
    "src/flow/",
    "src/flow/lib/current-flow-state.js",
    "src/flow/lib/canonical-flow-runtime.js",
    "src/lib/flow-version.js",
    "src/lib/flow-workspace.js",
    "tests/",
    "specs/"
  ],
  "tasks": [
    {
      "id": "T-1",
      "title": "Define the query contract",
      "goal": "Schema Revision 1 の request、resource-specific success/error response、Timestamp、SelectedFlowVersion、MetadataItem、ActivityItem、ArtifactDescriptor、AggregateMetrics、cursor、public-safe error path を centrally managed contract として定義し、metadata / activities の cardinality、exact keys、types、nullability、source mapping、stable error code を固定する。",
      "acceptance": [
        "metadata success は `item`、activities success は `items` / `pageInfo` を持つ。",
        "known-resource error と resource 不明 error の payload shape が定義される。",
        "error code 集合が CLI、fixture、consumer compatibility test から共有される。"
      ],
      "implementation_notes": "既存 generic Flow Envelope と混同しない query-specific class / schema を設ける。Schema Revision 1 の success top-level trees は metadata `{ schemaRevision: 1, ok: true, resource: \"metadata\", selectedFlowVersion: SelectedFlowVersion, availableFlowVersions: integer[], item: MetadataItem }` と activities `{ schemaRevision: 1, ok: true, resource: \"activities\", selectedFlowVersion: SelectedFlowVersion, availableFlowVersions: integer[], items: ActivityItem[], pageInfo: { limit: integer, endCursor: string|null, hasNext: boolean } }` の exact keys とする。`MetadataItem` は `{ identity, lifecycle, location, structure, relationships, timestamps, capabilities, artifacts, metrics }`、`ActivityItem` は R11 の exact whitelist、`ArtifactDescriptor` は R7 の exact whitelist、Timestamp は `{ value: ISO8601|null, availability, reason, provenance }` とし、cost は provider が amount と currency を実測した場合だけ公開する。identity/selection は FlowVersion/FlowSpecIdentity、state/lifecycle/blocked は validated CurrentFlowState、timestamps は confirmed Activity timing、activities は confirmed prefix、metrics は ActivityUsage/ActivityMetric、artifacts は validated catalog descriptor/association から写像する。error の `path` は public request domain のみとし、内部 path/name/body/raw catalog を success/error のどこにも含めない。",
      "test_strategy": "contract unit test と JSON shape fixture で success / error の exact fields を検証する。",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-2",
      "title": "Implement request parsing",
      "goal": "stdin と `--request-file` の入力境界を実装し、単一 JSON object と resource-specific request value object を生成する。",
      "acceptance": [
        "stdin / `--request-file` のどちらか一方だけを受理する。",
        "multiple JSON values、trailing value、unknown field、resource 不適合 field を拒否する。",
        "stdout は response JSON のみで診断は stderr 側に分離される。"
      ],
      "implementation_notes": "JSON parser と request class の constructor で boundary invariant を強制し、request に schema revision / cardinality / order を持たせない。",
      "test_strategy": "CLI integration test で valid input、invalid JSON、入力競合、trailing data、exit status を検証する。",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-3",
      "title": "Discover and select Versions",
      "goal": "対象 `specId` の canonical Version を発見し、明示または default Version を fallback なしで選択する。Version 1 専用 runtime facade の制約を query discovery に漏出させない。",
      "acceptance": [
        "available Version が unique ascending で返る。",
        "省略時は Version 1、明示時は指定 Version のみを読む。",
        "Version 2 以降の canonical fixture を Version 1 hardcode の変更なしで選択できる。"
      ],
      "implementation_notes": "`CanonicalFlowVersionReader` は `listAvailableVersions(specId)` で canonical `FlowVersionLocation` を unique ascending に列挙し、`open(specId, flowVersion)` で指定 identity を fallback なしに開く。open は state/spec/confirmed-ledger/catalog の共通 validation boundary を Version 1 以上に適用し、`FlowVersion` の directory identity と state format revision を混同しない。old layout、migration classifier/report、Version 1 専用 `CurrentFlowVersionStore` facade は参照せず、query read mode では lock materialization を含む filesystem mutation を行わない。",
      "test_strategy": "Version 1、Version 2 以降、missing Spec、missing Version、複数 Version の integration fixture を検証する。",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-4",
      "title": "Project metadata",
      "goal": "canonical state、Spec record、Artifact catalog から一つの public metadata `item` を生成する。lifecycle、derived blocked、timestamp availability、metrics を authority-backed に表現する。",
      "acceptance": [
        "identity、location、structure、relationships、capabilities、artifacts、metrics が public schema に従う。",
        "creation / update / finalize timestamp の value、availability、provenance が証拠の有無を表す。",
        "Artifact descriptor に deterministic ID と relation だけが現れ、body / internal path が現れない。"
      ],
      "implementation_notes": "confirmed Activity prefix を confirmationOrder 昇順に一度だけ走査する。created は最初の `flow_created`、updated は state-changing transition の最新、finalized は `finalize_flow` の最新の `timing.finishedAt` を使い、該当なしは non-empty reason/provenance 付き unavailable とする。AggregateMetrics は `{ activityCount: non-negative integer, durationMs: non-negative integer, inputTokens: non-negative integer, outputTokens: non-negative integer, cacheReadTokens: non-negative integer, cost: { amount: non-negative number, currency: string }|null }` とし、duration/usage は non-null measured values のみ加算、counter metric は reset 後の delta を一度だけ加算し、usage と同じ観測を metric で二重計上しない。cost は amount/currency が同一 provider measurement に存在する場合だけ採用し、token/model/time から推計しない。",
      "test_strategy": "fresh / migrated、creation evidence 有無、active / parked / blocked-derived / finalized、Artifact relation、metrics の contract test を実施する。",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-5",
      "title": "Project Activities",
      "goal": "confirmed Activity prefix を public Activity item に変換し、`timing.finishedAt` による datetime filter と `confirmationOrder` 昇順を適用する。",
      "acceptance": [
        "Activity の required public fields と nullable fields が安定して返る。",
        "filesystem timestamp ではなく `timing.finishedAt` で `gte` / `lt` が評価される。",
        "zero match が success の空 `items` になる。"
      ],
      "implementation_notes": "raw logs、prompt bodies、checkpoint、transaction、lock、temporary data、Artifact bodies は projector の入力・出力境界で遮断する。",
      "test_strategy": "Activity field shape、confirmed prefix、timezone bounds、zero match、各 outcome / failure / reference の fixture を検証する。",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-6",
      "title": "Add cursor pagination",
      "goal": "activities の opaque cursor を実装し、query 条件と最後の `confirmationOrder` に binding した gap-free pagination を提供する。",
      "acceptance": [
        "page limit 1..100 と initial after null を検証する。",
        "cursor は resource、selector、datetime bounds、last order を deterministic に保持する。",
        "malformed cursor と別 query / 改変 cursor を異なる machine-readable error にする。"
      ],
      "implementation_notes": "Cursor class は versioned canonical JSON payload を base64url serialize し、decode 後に exact fields と binding を検証する。offset pagination は追加しない。",
      "test_strategy": "複数 page の欠落・重複、hasNext / endCursor、cursor mismatch、active ledger append stability を integration test する。",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-7",
      "title": "Wire the query command",
      "goal": "query-specific request / reader / projector / response を top-level Flow command routing に接続する。既存 Flow command の behavior を維持する。",
      "acceptance": [
        "`sennel flow query` が metadata / activities の両 resource を dispatch する。",
        "success は code 0、`ok: false` は non-zero で終了する。",
        "既存の少なくとも一つの Flow command behavior が routing 変更前後で維持される。"
      ],
      "implementation_notes": "`flow query` の adapter は stdin/`--request-file` を検証して read-only query service を直接 dispatch し、成功・失敗とも query-specific Schema Revision 1 response の JSON 一つだけを stdout に出し、診断を stderr、`ok: false` を non-zero exit に分離する。generic Flow Envelope、ambient lifecycle、runtime-log、post-hook を通らず、state / ledger / catalog / cache / Git mutation API を呼ばない。",
      "test_strategy": "CLI end-to-end fixture と既存 Flow command の regression test を実施する。",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-8",
      "title": "Build contract fixtures",
      "goal": "Issue #517 の fresh / migrated、lifecycle、Git、Version、error、pagination 条件を canonical fixture として整備する。",
      "acceptance": [
        "fresh / migrated、Git あり / なし、active / parked / blocked-derived / finalized を含む。",
        "missing creation evidence、Version 2 以降、unreadable / inconsistent record、invalid request / cursor を含む。",
        "canonical fixture の入力は query 実行前後で変更されない。"
      ],
      "implementation_notes": "fixture は canonical Store の public authority を組み立て、old layout の query parsing をテスト依存にしない。",
      "test_strategy": "behavior-level acceptance fixture と canonical digest / Git status snapshot を組み合わせる。",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-9",
      "title": "Verify consumer compatibility",
      "goal": "Workspace / Connector consumer 相当の compatibility fixture を追加し、Schema Revision 1 の全 success / known-resource error と internal boundary を検証する。",
      "acceptance": [
        "consumer は supported `schemaRevision` を受理し unsupported revision を拒否する。",
        "metadata consumer は exactly one `item`、activities consumer は `items` / `pageInfo` と Version fields を利用できる。",
        "consumer は internal path、raw catalog、generic Flow Envelope fields に依存しない。"
      ],
      "implementation_notes": "production response schema を fixture と consumer test が直接共有し、重複した shape 定義を作らない。",
      "test_strategy": "consumer compatibility integration test と全 response/error fixture の round-trip assertion を実施する。",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    }
  ]
}
